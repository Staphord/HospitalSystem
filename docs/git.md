# Git Workflow

```powershell
# Stage all changes
git add .

# Commit with message
git commit -m "Save local changes before pulling main"

# Pull latest from remote main
git pull origin main

# Merge into current branch
git merge origin/main

# Push to remote branch
git push origin nasr
```
