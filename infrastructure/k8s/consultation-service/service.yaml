apiVersion: v1
kind: Service
metadata:
  name: consultation-service
spec:
  selector:
    app: consultation-service
  ports:
    - protocol: TCP
      port: 8012
      targetPort: 8012
  type: ClusterIP
